import turtle
from turtle import *
import math
def square(l):
    speed(0)
    ang=range(45,406,20)
    seth(90)
    up()
    setpos(200,0)
    down()
    for angl in ang:
        seth(angl)
        for rep in range(6):
          fd(l)
          lt(90) 
square(50)
done()